@model TaskManager.Models.TaskItem

@{
    ViewData["Title"] = "Task Details";
}

<h1>Task Details</h1>

<div>
    <h4>Task</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Title</dt>
        <dd class="col-sm-10">@Model.Title</dd>

        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Description</dd>

        <dt class="col-sm-2">Status</dt>
        <dd class="col-sm-10">@Model.Status</dd>

        <dt class="col-sm-2">Priority</dt>
        <dd class="col-sm-10">@Model.Priority</dd>

        <dt class="col-sm-2">Created At</dt>
        <dd class="col-sm-10">@Model.CreatedAt.ToString("g")</dd>

        <dt class="col-sm-2">Updated At</dt>
        <dd class="col-sm-10">@Model.UpdatedAt.ToString("g")</dd>

        <dt class="col-sm-2">Updated At</dt>
        <dd class="col-sm-10">@Model.DueDate?.ToString("g")</dd>

        <dt class="col-sm-2">User</dt>
        <dd class="col-sm-10">@Model.User?.Name</dd>

        <dt class="col-sm-2">Project</dt>

        <dt class="col-sm
